# Deny all ingress to bifrost by default
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: bifrost
spec:
  podSelector: {}
  policyTypes:
    - Ingress
